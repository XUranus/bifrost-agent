//! FileBackupAdapter: wraps bifrost Scanner + BackupTask + RestoreTask.

/// Adapter for file-level backup and restore operations via the bifrost engine.
pub struct FileBackupAdapter;

impl FileBackupAdapter {
    pub fn new() -> Self {
        Self
    }
}

// Stub — full implementation will be filled in during Week 3-4.
